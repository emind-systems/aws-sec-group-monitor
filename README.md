aws-sec-group-monitor
=====================

A script that monitor changes in EC2 security groups and writes the changes to Syslog

Usage: ./aws-sec-group-monitor.sh -O <aim-key> -W <aws-secret> [-r all|<region-url>]"


